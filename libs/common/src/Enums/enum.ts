export enum ApprovalStatus {
  AWAITING = '1',
  ACCEPT = '2',
  REJECT = '3',
}

export enum Gender {
  MALE = '1',
  FEMALE = '2',
}

export enum UserType {
  SELLER = '1',
  CUSTOMER = '2',
}

export enum HideHelpStatus {
  TRUE = '1',
  FALSE = '0',
}

export enum ActiveStatus {
  INACTIVE = '0',
  ACTIVE = '1',
}
